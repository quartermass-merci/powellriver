// Paste your Firebase web config here (see README.md, step 2).
// Leave as-is (all empty strings) to run in LOCAL-ONLY mode — the app stores to
// localStorage instead of Firebase. Useful for offline dev or if you haven't
// set up Firebase yet.
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  databaseURL: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};

// Which trip document to read/write. Change if you run multiple trips off the
// same Firebase project.
window.TRIP_ID = "meridian-powell-river";
